# Multi-Component Design 方法论

当一个软件任务需要组合多个组件（库、框架、服务、工具）协作完成时，协作方式决定了对每个组件的能力要求——必须先想清楚组件如何配合，才能知道各自需要满足什么条件。在确定方案前按以下顺序梳理：

1. **设计协作方式**：明确各组件之间的配合模式（数据流向、调用关系、职责边界）
2. **反推组件要求**：从协作方式推导出对每个组件的具体要求（需要提供什么 API/能力/数据格式）
3. **验证可行性**：检查每个组件是否能满足这些要求（查文档、读源码、确认版本支持）
4. **识别缺口与调整**：如有组件无法满足要求，明确指出缺口，然后决定——换组件、扩展组件、还是调整协作方式以绕过限制

先把这个分析呈现给用户，确认后再执行。

## 示例

实现带说话人标注的语音转录，拆为两个组件——A 做说话人识别（输出每个说话人的时间区间），B 做语音转文字。协作方式有两种选择：

- **方式一**：B 全量转录完整音频，A 独立输出说话人时间区间，最后通过时间戳对齐合并。这要求 B 的输出必须携带时间戳——如果 B 只输出纯文本不带时间信息，就无法与 A 的切片对齐，方案不可行。
- **方式二**：A 先切出音频片段，再把每个片段分别喂给 B 转录。这种方式下 B 不需要输出时间戳，但要求音频切割精度足够高且 B 能接受任意长度的音频输入。

不同协作方式对组件的要求完全不同。先确定配合方式，才能验证组件是否满足条件。
